PORT=3000

# MongoDB Atlas. La usan make up-atlas, make prod-up y la app sin Docker.
# make up la ignora: usa el Mongo local que levanta docker-compose.yml.
MONGODB_URI=mongodb+srv://USER:PASS@HOST/ordenalink?retryWrites=true&w=majority

JWT_SECRET=cambia-esto
GOOGLE_CLIENT_ID=
